# Pocket — environment variables

# Django
SECRET_KEY=change-me-to-something-random
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (prod only — dev uses SQLite)
DB_NAME=pocket
DB_USER=pocket
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432
